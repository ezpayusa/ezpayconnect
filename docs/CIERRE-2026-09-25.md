# Cierre de sesión — 25-sep-2026

P1, P2 y P3 del recon FHIR (`docs/PENDIENTES-RECON-FHIR-2026-09-24.md`) quedan **cerrados**: aplicados en prod,
verificados en sesión independiente y mergeados a main. P4 sigue abierto. Todo lo de abajo está en `origin/main`.

## Migraciones de la sesión

| Mig | Qué hace | md5 archivo / rollback | Probes | Commits → merge |
|---|---|---|---|---|
| 329 | El despacho rechaza recetas canceladas (PR010) en las 6 funciones de despacho; `confirmar_recepcion_receta` sin tocar | `c54a1a45…` / `711add8f…` | P840–P847 | `5d97314` → `9a4e9f4` |
| 330 | Rangos de plausibilidad de signos vitales: validación en `capturar_signo_vital` (SV001 fuera de rango, SV002 formato de PA) + 8 CHECK `sv_*_rango`; unidades visibles en captura y superficies; asistente-ia con unidades | `868448c8…` / `615ebf55…` | P848–P860 | `d7cbe4d` + `f784ef7` → `23801a8` |
| 331 | Toma de vitales vacía rechazada (SV003); borra la fila QA 1453; el form detecta números inválidos (badInput) | `b50a308a…` / `03ad1021…` | P861–P865 | `d2a41b7` + `a9749d8` → `34e4490` |
| 332 | P3 aditiva: `examenes.catalogo_id` (FK RESTRICT, backfill 10/4), RPCs `crear_orden_examen_medico` / `crear_orden_examen_walkin`, congelamiento de `tipo`/`catalogo_id` (EX022), grants por columna, UNIQUE del catálogo, renombre bloqueado; front por RPC con marca "fuera de catálogo" | `71429b50…` / `9c472549…` | P866–P878 | `731e294` + `6e3d3ef` → `4477dd6` |
| 333 | P3 cierre: sin INSERT directo de órdenes; policies ALL partidas por comando; sin TRUNCATE/TRIGGER/REFERENCES para authenticated/anon | `b40cf640…` / `aa059328…` | P879–P884 (+ P878 actualizada) | `9bda231` → `6a0ebfa` |

P1 (mig 328, emisión de recetas solo por `emitir_receta`) se cerró antes en la misma jornada: merge `f142d46`.

`docs/PENDIENTES-RECON-FHIR-2026-09-24.md` se trackea por primera vez en este commit (antes vivía solo en disco).

## Estado verificado

- **Harness** contra prod: VERDE, **959 filas**, 0 vacíos, **11 rojas** (todas en la deuda), P840–P884 OK; **guard** `do_sin_handler = 155`; **grants** P800 PASA.
- **Edge `asistente-ia`**: v28, `verify_jwt = true`, fuente viva = main (unidades explícitas en el contexto de la IA).
- **Vercel** (ezpayconnect-app): Production en success sobre cada merge.

## Próximos números libres

Probe **P885** · migración **334** · errcodes **EX023** (exámenes), **SV004** (vitales), **PR011** (recetas), **PA035** (comercial).

## Backlog nuevo (no iniciado)

- **MAINTAIN (PG17)**: `authenticated` lo tiene en 109/121 tablas de `public` y `anon` en 8 (incl. `pacientes`, `recetas`). P800 no lo ve porque mira `role_table_grants`. Frente aparte.
- **DELETE directo en exámenes/órdenes**: el laboratorio (y médico/super_admin) conserva DELETE por policy; ninguna pantalla lo usa. Decidir si se cierra.
- **Refresco del panel de exámenes** de la consulta: no se vuelve a pedir la lista después de crear una orden (comportamiento previo a P3).
- **Laboratorio preferido en el modal**: el modal abre sin laboratorio hasta que responde `laboratorios_para_medico`; lo que se tipea en "Tipo de examen" en ese intervalo queda oculto e ignorado.
- **Mensaje de peso en lb**: el error de rango del peso habla en kg aunque el input esté en lb.
- **`.gitattributes eol=lf`** para migraciones (el md5 del autochequeo asume LF; hoy depende de `core.autocrlf`).
- **Fixture del médico con proveedor pendiente**: dentro del harness, otro fixture le deja al médico QA una cuenta de proveedor con empresa `pendiente`; P870 lo esquiva, pero conviene aislarlo.

## Ramas de respaldo

- `feat/reveal-r2-walkin-qr` (7ed317b) y `feat/reveal-r3-bandeja` (7dcb129): checkpoints de junio de otra implementación de 156/157, superados por lo que está en main; subidos a origin solo como respaldo, NO mergear. El `.gitattributes eol=lf` de 7dcb129 sirve de referencia para el ítem del backlog.
